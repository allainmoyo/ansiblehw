---
- name: install nginx webserver
  hosts:
    - centos1
  become: yes

<<<<<<< HEAD
  roles:
    - nginx
=======
    - name: Install EPEL repo if CentOS
      yum:
        name: epel-release
        state: present
      when: ansible_distribution == 'CentOS'
      tags:
        - [ never, nstall_nginx ]

    - name: install nginx
      package:
        name: nginx
        state: present
      tags:
        - [ never, install_nginx ]

    - name: uninstall nginx
      package:
        name: nginx
        state: absent
      tags:
        - [ never, uninstall_nginx ]

    - name: stop nginx
      service:
        name: nginx
        state: stopped
      tags:
        - [ never, stop_nginx ]

    - name: start nginx
      service:
        name: nginx
        state: started
      tags:
        - [ never, start_nginx ]

    - name: enable autostart
      service:
        name: nginx
        enabled: yes
      tags:
        - [ never, enable_nginx ]

    - name: disable autostart
      service:
        name: nginx
        enabled: no
      tags:
        - [ never, enable_nginx ]
>>>>>>> d8aa50fca3e41ce5ffe721acdcb6fe065ff11fe9
